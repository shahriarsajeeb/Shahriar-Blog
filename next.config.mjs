/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns: [
            {
                hostname:"shahriarblog.oneentry.cloud"
            }
        ]
    }
};

export default nextConfig;
